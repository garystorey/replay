export type SnackboxProps = {
  user: number
  playback: number
  id: string
  animation?: string
  className?: string
  // eslint-disable-next-line no-unused-vars
  onClick?: (btns: string) => void
}
